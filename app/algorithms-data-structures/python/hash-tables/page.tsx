import PlaceholderPage from "@/components/PlaceholderPage";

export default function HashTablesPage() {
  return (
    <PlaceholderPage
      title="Hash tables in Python"
      description="We will cover dictionaries, counting patterns, and collision intuition."
    />
  );
}
