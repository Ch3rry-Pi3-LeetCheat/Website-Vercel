import PlaceholderPage from "@/components/PlaceholderPage";

export default function ContactPage() {
  return (
    <PlaceholderPage
      title="Contact page in build"
      description="We will add a contact form once the course structure is finalized. For now, reach out through your usual channels."
    />
  );
}
